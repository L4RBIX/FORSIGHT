class HomographyProjector:
    """Placeholder for camera-pixel to table-coordinate projection."""

    def project(self, pixel_xy: tuple[float, float]) -> tuple[float, float, float]:
        x, y = pixel_xy
        return (x, y, 0.0)
