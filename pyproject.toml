[project]
name = "foresight-pm"
version = "0.1.0"
description = "Offline-first physical world model and safety oracle for tabletop robot commands"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
